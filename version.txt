VERSION_MAJ   = 1
VERSION_MIN   = 7
VERSION_PATCH = 2
VERSION_TYPE = a        # a = alpha, b = beta, rc = release candidate, r = release, other => error
VERSION_BUILD = 240311  # for non-release builds
